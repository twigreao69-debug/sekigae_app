name: Build Android APK (Latest Version)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4 # v3からv4に更新

      - name: Set up Node.js
        uses: actions/setup-node@v4 # v3からv4に更新
        with:
          node-version: 18

      - name: Set up Java
        uses: actions/setup-java@v4 # v3からv4に更新
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Force Fix Dependencies
        run: |
          npm init -y
          npm install @capacitor/core @capacitor/cli @capacitor/android
          npx cap init "席替え" com.sekigae.app --web-dir .

      - name: Add Android Platform
        run: |
          npx cap add android
          npx cap copy android

      - name: Build APK with Gradle
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4 # v3からv4に更新
        with:
          name: sekigae-app-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
